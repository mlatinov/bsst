% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bsst_plot_stress_2d.R
\name{bsst_plot_stress_2d}
\alias{bsst_plot_stress_2d}
\title{Plot Recovery/Stress Metric Across Two Design Variables (Heatmap)}
\usage{
bsst_plot_stress_2d(
  x,
  x_var,
  y_var,
  parameter,
  fill_var = c("stress_score", "std_error", "error"),
  fix_others = NULL,
  palette = "dark_research",
  engine = c("ggplot2", "plotly"),
  show_nonconverged_marker = TRUE,
  title = NULL
)
}
\arguments{
\item{x}{Output list from \code{\link{bsst_stress}} or \code{\link{bsst_bo}}.}

\item{x_var, y_var}{Character; names of the two design variables for the
horizontal/vertical axes.}

\item{parameter}{Character; single tracked parameter name. Required.}

\item{fill_var}{One of \code{"std_error"}, \code{"error"},
\code{"stress_score"}. Default \code{"stress_score"}.}

\item{fix_others}{Named list of fixed values for any other varying
design variables, required whenever more than \code{x_var}/\code{y_var}
vary in the underlying design. Numeric variables use nearest-match;
non-numeric require an exact match.}

\item{palette}{One of \code{"dark_research"}, \code{"github_dark"},
\code{"scientific"}.}

\item{engine}{One of \code{"ggplot2"}, \code{"plotly"}.}

\item{show_nonconverged_marker}{Logical; if \code{TRUE} (default), tiles
corresponding to a non-converged fit are marked with a small "x"
overlay rather than left visually identical to converged tiles.}

\item{title}{Optional plot title.}
}
\value{
A ggplot2 or plotly object depending on \code{engine}.
}
\description{
Visualizes a recovery metric as a 2D heatmap across two simulation
design variables, for a single tracked parameter. Answers: "is there an
interaction between two simulation conditions that jointly break
recovery — e.g. small sample size is fine unless measurement error is
also high?"
}
