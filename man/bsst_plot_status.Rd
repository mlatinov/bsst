% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bsst_plot_status.R
\name{bsst_plot_status}
\alias{bsst_plot_status}
\title{Plot Recovery Status Across Two Design Variables (All Tracked Parameters)}
\usage{
bsst_plot_status(
  x,
  x_var,
  y_var,
  parameters,
  fix_others = NULL,
  palette = "dark_research",
  title = NULL
)
}
\arguments{
\item{x}{Output list from \code{\link{bsst_stress}} or \code{\link{bsst_bo}}.}

\item{x_var, y_var}{Character; the two design variables for the axes.}

\item{parameters}{Character vector of tracked parameters to facet over.
Required — must be supplied explicitly.}

\item{fix_others}{Named list of fixed values for other varying design
variables. Numeric variables use nearest-match.}

\item{palette}{One of \code{"dark_research"}, \code{"github_dark"},
\code{"scientific"}.}

\item{title}{Optional plot title.}
}
\value{
A ggplot2 object.
}
\description{
Tile plot of the categorical \code{recovery_status} (not a collapsed
scalar) across two design variables, faceted by parameter. Answers:
"across my design space, where does each tracked parameter individually
fall into each qualitative recovery bucket?" — deliberately not
collapsed to a single stress score, since averaging/maxing across
parameters (as \code{stress_score} does) can hide that, say, beta
recovers everywhere while sigma fails only when n is small.
}
